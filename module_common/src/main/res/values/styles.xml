<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item><!--//无标题-->
        <item name="android:windowActionBar">false</item><!--//无ActionBar-->
        <item name="android:windowFullscreen">true</item><!--//全屏即无通知栏-->
        <item name="android:windowContentOverlay">@null</item><!--//是否有遮盖-->
        <item name="android:windowBackground">@drawable/splash_bg_welcome</item><!--启动白屏-->
    </style>


    <!-- dialog -->
    <style name="Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!-- 默认水平线 -->
    <style name="ViewDefaultLine">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1px</item>
        <item name="android:background">#e4e4e4</item>
    </style>

    <!-- 默认垂直线 -->
    <style name="ViewDefaultLineVertical">
        <item name="android:layout_width">1px</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">#e4e4e4</item>
    </style>

</resources>
